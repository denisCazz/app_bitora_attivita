name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: rapportini
          POSTGRES_PASSWORD: rapportini
          POSTGRES_DB: rapportini
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rapportini"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://rapportini:rapportini@localhost:5432/rapportini
      JWT_ACCESS_SECRET: ci-access-secret-change-me
      JWT_REFRESH_SECRET: ci-refresh-secret-change-me
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @rapportini/api exec prisma generate
      - run: pnpm --filter @rapportini/api exec prisma migrate deploy
      - run: pnpm typecheck
      - run: pnpm --filter @rapportini/shared test
